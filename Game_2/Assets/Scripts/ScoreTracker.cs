using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class ScoreTracker : MonoBehaviour { /*
    int highestTile = 2;
    int score = 0;

void Start(){
    TileScript tile = GetComponent<TileScript>();
}
    

//may have to be the built in update method
public void MainScore(){
    if (!tile.notYetMerged){   //using value from TileScript script
        score += tile.value;
    } else {
        tile.notYetMerged = true;
    }
}

//also may have to be in the update method
public void HighestTile(Rigidbody2D other){
    if(other.tile.value > tile.value)
        highestTile = other.tile.value;  
}
*/
}