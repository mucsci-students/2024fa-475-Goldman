using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class ScoreTracker : MonoBehaviour { /*
    int highestTile = 2;
    int score = 0;
    Rigidbody2D tile1;   //make whatever the tiles are listed as when seeing if
    Rigidbody2D tile2;   //two tiles can/have merged together to look at scores
    

//may have to be the built in update method
public void MainScore(){
    if (!notYetMerged){   //using value from TileScript script
        score += new tile number;
    } else {
        notYetMerged = true;
    }
}

//also may have to be in the update method
public void HighestTile(Rigidbody2D newTile){
    if(newTile > highestTile)
        highestTile = newTile.value;  
}
*/
}